let salaries = {
    andrey: 500,
    sveta: 413,
    anton: 987,
    alexandra: 199
};

let stotal = 0;
let empno = 0;

for (let employee in salaries) {
    stotal += salaries[employee];
    empno++;
}
let averageSalary = stotal / empno;

console.log(averageSalary);